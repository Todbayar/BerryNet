#include <Arduino.h>
#include <WiFi.h>

const char* ssid = "Univision_3843";
const char* password = "db0fb09b8950";

void initWiFi() {
    Serial.begin(115200);
    btStop();
    WiFi.mode(WIFI_STA);
    WiFi.begin(ssid, password);
    Serial.print("Connecting to WiFi ..");
    while (WiFi.status() != WL_CONNECTED);
    Serial.println(WiFi.localIP());
}

void setup() {
    initWiFi();
}

void loop() { 
}